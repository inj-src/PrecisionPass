import { AppLayout } from "@/components/layout/app-layout";

export default function FaceRegistrationLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <AppLayout>{children}</AppLayout>;
}
